models:
  - disks:
      - disk:
          device: "#disk#"
          size: "#size#"
          gpt: false
          partitions:
            - partition:
                name: "boot"
                mount: "/boot"
                filesystem: "#system#"
                start: "1MiB"
                end: "200MiB"
